apply plugin: 'groovy'
apply plugin: 'bintray-release'

buildscript {
    repositories { jcenter() }
    dependencies {
        classpath 'com.novoda:bintray-release:0.3.4'
    }
}

repositories {
    jcenter()
}

dependencies {
    compile gradleApi()
    compile localGroovy()
    compile 'com.android.tools.build:gradle:2.1.0'

    testCompile 'junit:junit:4.12'
    testCompile 'com.google.truth:truth:0.28'
}

publish {
    userOrg = 'sturdycode'
    groupId = 'com.sturdycode'
    artifactId = 'catalog'
    version = '1.0.0-SNAPSHOT'
    description = 'A gradle plugin for Android that records logcat when running conected tests'
    website = 'https://github.com/SturdyCode/catalog'
    uploadName = 'Catalog'
}